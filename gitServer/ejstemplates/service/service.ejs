import API from "../api/<%= moduleName %>.js";

export default {
<%_ data.forEach(function(row){ -%>
    <%= row.servicesName %>(context,cb){
        API.<%= row.servicesName %>(context)
            .then(x => {
                cb && cb(x.data);
            })
            .catch(error => {
                context.alert.showAlert("error",e.data);
            })
    },
<%_ _%><% }) -%>
}
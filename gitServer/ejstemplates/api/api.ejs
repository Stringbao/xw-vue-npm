let _map = {
<%_ data.forEach(function(row){ -%>
    <%= row.compName %>:"<%= row.url %>",
<%_ _%><% }) -%>
};
export default {
<%_ data.forEach(function(row){ -%>
    <%= row.servicesName %>(context){
        return context.ajax.getFetch(_map.<%= row.compName %>);
    },
<%_ _%><% }) -%>
}